'use client'
import Home from '../page'
import styles from './dashboard.module.css'

import React, { Suspense } from 'react'
const page = () => {

    return (
        <div > hola mundo
            <Home />
        </div>
    )
}

export default page